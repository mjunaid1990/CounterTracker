modules = ["flutter"]

[nix]
channel = "stable-24_05"
packages = ["wget", "flutter"]

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Flutter Counter App"

[[workflows.workflow]]
name = "Flutter Counter App"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd $PWD && flutter run -d web-server --web-port=5000 --web-hostname=0.0.0.0"
waitForPort = 5000

[deployment]
run = ["sh", "-c", "flutter run -d web-server --web-port=5000 --web-hostname=0.0.0.0"]

[[ports]]
localPort = 5000
externalPort = 80

[[ports]]
localPort = 8080
externalPort = 8080
